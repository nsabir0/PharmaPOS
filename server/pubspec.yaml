name: pharma_pos_server
description: "Backend for Smart Pharmacy POS built with Dart Frog and PostgreSQL."
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  dart_frog: ^1.2.6
  postgres: ^3.5.9
  logger: ^2.7.0

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.24.9
